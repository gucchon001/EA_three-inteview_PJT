APP_ENV=local
APP_NAME=FastAPI Base
APP_VERSION=local
APP_AUTH_MODE=mock
APP_ALLOWED_EMAIL_DOMAIN=example.com

# Add real secret values only in local .env or runtime secret injection.
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
# DATABASE_URL=


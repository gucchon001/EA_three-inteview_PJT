/**
 * One-shot: PJC シートへ project-charter レイアウトで書き込み（gws）。
 * gws-params-encoding: cmd /c + --params / --json を別引数で渡す。
 */
import { spawnSync } from 'child_process';

/** cmd 経由だと長い --json が壊れるため、run-gws.js を process.execPath で直接起動する（gws-params-encoding） */
const RUN_GWS =
  process.env.GWS_RUN_JS ||
  'C:/Users/yohay/AppData/Roaming/npm/node_modules/@googleworkspace/cli/run-gws.js';
const SPREADSHEET_ID = '1SM9kTEDDNz0zDAvsmXV0MaX14CpQg90BJQPcdHD40JM';
const SHEET = 'PJC';

// docs/project/面談学習計画レポート標準化_PJC.md より（値は C・E・H を中心に）
const C5 = [
  '【現状（AsIs）】',
  '面談メモ／学習計画が属人的で、準備に 4〜6 時間／件かかる。教師ミーティング約 60 分・家庭面談 30〜60 分に加え、前後準備が重い。学校ごとにシラバスや運用の差はあるが、シラバスマッピング（IBO 連携等）は別プロジェクトで扱い、本 PJ では当面そこに依存しない前提で進める。開発は面談メモ／学習計画ビューと MOOC 中心。教材・教師連携は停滞し優先度低下。スケジュールはテスト時期・担当変更・順番変更などイレギュラーが多く、システム化と手動の線引きが未決。顧客向けコミュニケーションはメール中心で、アップセルにはレポート（PDF 等）の方が有効という認識。模試・答案・日程などの情報共有が遅延・未共有になりやすい。週次指導報告は手作業が多く、月次サイン・給与連携とサイクルが必ずしも一致しない。',
  '',
  '【あるべき姿（ToBe）】',
  'ビューを「面談メモ」「学習計画表」に集約し、編集・出力を簡便化。テンプレを複数（学校ベース・サブトピック数ベース等）用意し、選択後にカスタマイズ。ブラックボックス化を避け構造を可視化。成果物は メール中心からレポート中心へ。教師ミーティング起点の 月次ルーチン（学習計画テンプレ → 教師 MTG → メール／家庭面談※順序は顧客都合で変動）で アップセルと離脱防止（接触回数確保）を両立。答案の採点自動化は別プロジェクトのため、本 PJ の成果物には含めない。週次は 録画 → 文字起こし → 報告書への自動流し込みを First step として段階導入。表現配慮が必要な箇所は 人手で補完。学習計画の情報設計は数学を先行しつつ、他科目へ展開可能な形にする。',
  '',
  '【ギャップ・原因の仮説】',
  'ツールとテンプレが分散し標準化されていないこと、要約品質の定義が言語化されていないこと、入力データの十分性が未検証なことが工数と品質のボトルネック。スケジュール変動はルール化より先に「基本枠組み」と例外処理の設計が必要。',
].join('\n');

const E5 = [
  '【卒業要件】',
  '1. 面談メモ／学習計画の 2 ビューについて、合意した定義の「基本機能」を試作〜レビュー可能な状態にし、以降のスケジュールロジック調整に進める土台があること。',
  '2. 顧客向けレポート（PDF 等）の体裁テンプレ v1 と、内部運用用の区分が定義され、試作サンプルがレビュー可能であること。',
  '3. 週次指導報告の自動生成について、First step（録画 → 文字起こし → 報告書）の設計叩き台とパイロット範囲が合意されていること。',
  '4. 未決事項一覧（法務・権限・SLA・承認フロー等）がオーナー付きで棚卸しされ、次四半期に持ち越す項目が明示されていること。',
  '',
  '【歓迎要件】',
  '・学習計画・面談メモ用テンプレのバリエーション案（シラバスマッピング連携は別 PJ）。',
  '・メールをレポート風に改善したテンプレ案。',
  '・効果測定ダッシュボードの指標案（アップセル率・離脱率・工数・レポート閲覧率）。',
].join('\n');

const C6 = [
  '1. 2 ビュー: テンプレ複数・選択後カスタマイズ・出力形式選択・文字ぼかし等の実務機能。',
  '2. レポート: 面談時にレポートと学習計画を連動表示する情報設計。',
  '3. ワークフロー: 教師 MTG アジェンダとメール／レポート項目の紐づけ、準備チェックリスト標準化、情報提供依頼フロー（期限・方法・メリット）。',
  '4. 自動化: 段階導入。First step は週次（文字起こし → 報告書）。月次はミーティング内容とレポートの統合運用ルールを別紙で整理。',
  '5. データ: 料金体系など定型情報を AI 入力用に整備。',
  '6. スコープ外（別 PJ または当面）: シラバスマッピング（IBO 連携含む）、採点・答案の自動化、三者面談の新規プロセス立ち上げ、教材の同時大型推進。',
].join('\n');

const C8 = [
  '【In】2 ビュー、レポート出力、週次報告フロー設計、チェックリスト・情報依頼、メール／レポート型化、学習計画の構造化（数学先行の展開方針）。',
  '',
  '【Out（別 PJ または当面）】シラバスマッピング本番、採点・答案の自動化、三者面談の新規スコープ、教材・教師連携の大型立て直し、全科目同時の完全自動化。',
].join('\n');

const C10 = [
  '資料: 週次会議 2026-03-26 議事メモ、ホワイトボード「三者面談 PJT」タイムライン（準備 → 教師 MTG → メール → 家庭面談）。',
  '体制メモ: 藤井さん同席でメール構造・アジェンダの解像度向上予定。品質基準の言語化で修正負荷を下げる。',
].join('\n');

const rows = [
  ['PJC', '', '', '', '', '', '', ''],
  [
    '対応',
    'プロジェクト名',
    '面談・学習計画 2 ビュー＆レポート標準化 PJ（週次 2026-03-26 合意を反映）',
    '',
    '',
    '',
    '前提・制約メモ',
    '文字起こし・録画の保存先・権限・同意は法務と合意が必要。LMS／DB／ドライブ連携は技術リスクあり、段階導入。',
  ],
  [
    'who',
    '責任者',
    '（プロダクトオーナー／PM を記載）',
    '実行担当者',
    '開発・カリキュラム・運用・教師折衝の担当を列挙',
    '',
    '',
    '',
  ],
  [
    'when',
    '開始日',
    '2026-03-26（合意ベース）',
    '終了予定日',
    '上期重点（効率化・アップセル）／下期に研修・IBE 標準化をプール — マイルストーンは WBS で週次更新',
    '',
    '',
    '',
  ],
  ['why', '背景', C5, '目的・成功状態', E5, '', '', ''],
  ['what・How', '内容・方法', C6, '', '', '', '', ''],
  [
    'How much',
    '予算\nコスト見積',
    '（別紙または経営合意後に記載）— AI API・ストレージ・ツール費用を WBS に紐づけて見積。',
    '人的リソース',
    '開発、カリキュラム、運用、教師／保護者向けコミュニケーション、法務レビュー（個人情報・録画・文字起こし）を列挙し WBS オーナーに割当。',
    '',
    '',
    '',
  ],
  ['where', '影響範囲\nスコープ', C8, '', '', '', '', ''],
  ['', '', '', '', '', '', '', ''],
  ['', 'その他\n\n会議場所、相談場所\n担当者の責任と権限など', C10, '', '', '', '', ''],
];

function runGws(values, range) {
  const paramsObj = {
    spreadsheetId: SPREADSHEET_ID,
    range: `${SHEET}!${range}`,
    valueInputOption: 'USER_ENTERED',
  };
  const params = JSON.stringify(paramsObj);
  const jsonBody = JSON.stringify({ values });
  const r = spawnSync(
    process.execPath,
    [RUN_GWS, 'sheets', 'spreadsheets', 'values', 'update', '--params', params, '--json', jsonBody],
    { encoding: 'utf8', shell: false, windowsHide: true, maxBuffer: 10 * 1024 * 1024 }
  );
  if (r.error) throw r.error;
  const out = (r.stdout || '') + (r.stderr || '');
  if (r.status !== 0) {
    console.error(out);
    process.exit(r.status ?? 1);
  }
  try {
    const j = JSON.parse(r.stdout || '{}');
    console.log(JSON.stringify({ updated: j.updatedRange, rows: j.updatedRows }, null, 2));
  } catch {
    console.log(r.stdout);
  }
}

// 11 行に揃える（sheet-layout の A1:H11）
while (rows.length < 11) rows.push(['', '', '', '', '', '', '', '']);
const grid = rows.slice(0, 11);

const payloadLen = JSON.stringify(grid).length;
console.error('payload chars:', payloadLen);

runGws(grid, 'A1:H11');
